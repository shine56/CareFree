<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.a73233.carefree">

    <uses-permission android:name="android.permission.CAMERA" />
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />
    <uses-permission android:name="com.android.alarm.permission.SET_ALARM" />

    <application
        android:name="org.litepal.LitePalApplication"
        android:allowBackup="true"
        android:icon="@mipmap/icon"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".me.view.SettingActivity"></activity>
        <activity android:name=".me.view.AbandonActivity" />

        <service
            android:name=".note.view.ClockService"
            android:enabled="true"
            android:exported="true" />

        <receiver
            android:name=".note.view.ClockReceiver"
            android:enabled="true"
            android:exported="true"
            android:process=":remote" />

        <activity android:name=".note.view.ClockActivity" />
        <activity android:name=".note.view.NoteWriteActivity" />
        <activity android:name=".diary.view.WriteDiaryActivity" />
        <activity android:name=".diary.view.LookDiaryActivity" />
        <activity android:name=".util.BigPhotoViewer" />
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <provider
            android:name="android.support.v4.content.FileProvider"
            android:authorities="com.example.a73233.carefree.util.fileprovider"
            android:exported="false"
            android:grantUriPermissions="true">
            <meta-data
                android:name="android.support.FILE_PROVIDER_PATHS"
                android:resource="@xml/file_paths" />
        </provider>
    </application>

</manifest>