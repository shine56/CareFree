<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.NoActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/sad1</item>
        <item name="colorPrimaryDark">@color/sad1</item>
        <item name="colorAccent">@color/sad1</item>
        <item name="colorControlActivated">#AC69DB</item>
    </style>
    <style name="MyTheme" parent="Theme.AppCompat.Light.NoActionBar">
        <item name="colorControlActivated">#AC69DB</item>
        <item name="colorControlNormal">#AC69DB</item>
        <item name="colorPrimary">@color/mainBg</item>
        <item name="colorPrimaryDark">@color/mainBg</item>
        <item name="colorAccent">#AC69DB</item>
        <item name="android:windowTranslucentStatus">true</item>
    </style>
    <style name="ActionSheetDialogStyle" parent="@android:style/Theme.Dialog">
        <!-- 背景透明 -->
        <item name="android:windowBackground">@android:color/transparent</item>
        <item name="android:windowContentOverlay">@null</item>
        <!-- 浮于Activity之上 -->
        <item name="android:windowIsFloating">true</item>
        <!-- 边框 -->
        <item name="android:windowFrame">@null</item>
        <!-- Dialog以外的区域模糊效果 -->
        <item name="android:backgroundDimEnabled">true</item>
        <!-- 无标题 -->
        <item name="android:windowNoTitle">true</item>
        <!-- 半透明 -->
        <item name="android:windowIsTranslucent">true</item>
        <!-- Dialog进入及退出动画 -->
        <item name="android:windowAnimationStyle">@style/ActionSheetDialogAnimation</item>
    </style>
    <!-- ActionSheet进出动画 -->
    <style name="ActionSheetDialogAnimation" parent="@android:style/Animation.Dialog">
        <item name="android:windowEnterAnimation">@xml/actionsheet_dialog_in</item>
        <item name="android:windowExitAnimation">@xml/actionsheet_dialog_out</item>
    </style>

</resources>
